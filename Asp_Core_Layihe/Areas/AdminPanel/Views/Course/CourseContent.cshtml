@*@model IEnumerable<CourseContent>
@{
    ViewData["Title"] = "CourseContent";
}

<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Course Content</h4>
                <div class="d-flex justify-content-between">
                    <a class="btn btn-dark text-white" asp-controller="Course" asp-action="Index">Go Back</a>
                    <a class="btn btn-success" asp-controller="Course" asp-action="Create">Create Content</a>
                </div>
                <div class="d-flex justify-content-end">
                    @if (ViewBag.SliderCount < 5)
                    {
                        <a class="btn btn-success" asp-controller="HomeSlider" asp-action="Create">Create Slider</a>
                    }

                </div>

                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>
                                    About
                                </th>
                                <th>
                                    HowToApply
                                </th>
                                <th>
                                    Certification
                                </th>
                                <th>
                                    Setting
                                </th>

                            </tr>
                        </thead>
                        <tbody>


                            @foreach (CourseContent courseContent in Model)
                            {
                                <tr>
                                    <td>
                                        @courseContent.AboutCourse
                                    </td>
                                    <td>
                                        @courseContent.HowToApply

                                    </td>
                                    <td>
                                        @courseContent.Certification
                                    </td>
                                    <td>
                                        <a class="btn btn-primary" asp-controller="Course" asp-action="Detail" asp-route-id="@courseContent.Id">
                                            <i class="mdi mdi-information-variant"></i>
                                        </a>
                                        <a class="btn btn-warning" asp-controller="Course" asp-action="Edit" asp-route-id="@courseContent.Id">
                                            <i class="mdi mdi-border-color"></i>
                                        </a>
                                        <a class="btn btn-danger"asp-controller="Course" asp-action="Delete" asp-route-id="@courseContent.Id"  >
                                            <i class="mdi mdi-delete-variant"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>*@

